@model IEnumerable<Notify.Models.Producto>

@{
    ViewBag.Title = "Index";
}

<h2>Afegeix un producte</h2>

<p>
    @Html.ActionLink("Crear un nou producte", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.precio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.qtt)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.precio) €
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.qtt)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.codigo }) |
            @Html.ActionLink("Esborrar", "Delete", new { id=item.codigo })
        </td>
    </tr>
}

</table>
